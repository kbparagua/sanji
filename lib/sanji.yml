cookbook: _standard

cookbooks:
  basic:
    recipes:
    - _gemfile
    - _postgresql
    - _figaro
    - _seedbank
    - _annotate
    - _haml
    - _frontend
    - _readme
    - _gemfile_cleanup

  standard:
    include:
    - _basic

    recipes:
    - ^_basic
    - _draper
    - _reform
    - _paloma
    - _simple_gems
    - _public_module
    - _admin_namespace

    optional:
    - _admin_namespace

    gems:
      global:
      - thin
      - paperclip
      - kaminari

      "development,test":
      - thin
      - quiet_assets
